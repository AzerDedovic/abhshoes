<% content_for :head do %>
<%= stylesheet_link_tag "admin.css" %> 
<% end %>
<div class="container">
	<div class="row">
		<div class="col-md-1">
		</div>
		<div class="col-md-10">
      <div class="error" style="margin-left:17px">
          <% if  @editBrand.errors.any? %>
          <h2 class="error-title">Errors:</h2>
          <ul>
            <% @editBrand.errors.full_messages.each do |message| %>
            <li>  <%= message %></li>
            <% end %>

          </ul>

          <% end %>
      </div>
			<div class="boxUser">
			<%= form_for :updateBrand, :url=>{:action => "updateBrand", :controller => "admin", :id => @editBrand.id} do |f| %>
      
      	

      		<div>
       		<p>Brand</p>
        	<div class="field delivery_field">
          	<%= f.text_field :brand, :name => "brand", class: "form-control", value: @editBrand.brand %>
        	</div> 
        	</div>
        	</br>

			
		
			<div class="btn-group" style="left: 455px; bottom: -183px;">

     			<%= link_to raw('<button type="button" class="btn btn-primary button_back" style="position: absolute; left: 120px;">Back</button>'), {:controller => "admin", :action => "brands" } %>

		  		<%= submit_tag "Save", class: "btn btn-primary button_save_user",  id: "Save" %>
      
        		
		  	</div>
		  	<% end %>
        	
      		</div>
		</div>
		<div class="col-md-1">
		</div>
	</div>
</div>

