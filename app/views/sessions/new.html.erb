<h1>Log in</h1>

<% if flash[:error] %>
	<p><%= flash[:error] %></p>
	<% end %>

<%= simple_form_for(:session, :url => 'create') do |f| %>

	<%= f.input :username, placeholder: "email" %></p>
	<%= f.input :password, placeholder: "password" %></p>
	<%= f.button :submit, "Log In" %>
	<% end %>
